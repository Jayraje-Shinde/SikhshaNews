import { Box, Typography } from '@mui/material';
const News = () => (
  <Box>
    <Typography variant="h4" fontWeight={700} color="primary.main" gutterBottom>
      EduSignal
    </Typography>
    <Typography color="text.secondary">Educational news, updates, and announcements.</Typography>
  </Box>
);
export default News;
